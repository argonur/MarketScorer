# MarketScorer

## Instalar paquetes necesarios

Dentro de la raíz del directorio **`MarketScorer/`** se encuentra el archivo `requirements.txt`

- Ejecutar desde la terminal el comando:

```bash
pip install requirements.txt
```
